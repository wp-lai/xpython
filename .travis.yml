language: python

python:
    - "2.7"
    - "3.5"
    - "nightly"

install:
    - pip install -r requirements.txt

before_script:
    - flake8 ./code/ --ignore=F811

script:
    - py.test ./code/ --doctest-modules

